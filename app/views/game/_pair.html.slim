.pair style='display:none;'
  = link_to image_tag(user1.photo.try(:url, :normalized)), picks_path(uuid: UrlEncoder.encode(user1.id, user2.id)), class: 'left'
  = link_to image_tag(user2.photo.try(:url, :normalized)), picks_path(uuid: UrlEncoder.encode(user2.id, user1.id)), class: 'right'
