.pair data-pair-id="#{user1.id}#{user2.id}" style='display:none;'
  = link_to image_tag(user1.photo.try(:url, :normalized)), '#', class: 'left'
  = link_to image_tag(user2.photo.try(:url, :normalized)), '#', class: 'right'
